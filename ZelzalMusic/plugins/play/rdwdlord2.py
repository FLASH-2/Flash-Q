import asyncio
import random
from ZelzalMusic import app
from pyrogram.types import (InlineKeyboardButton,
                            InlineKeyboardMarkup, Message)
from pyrogram import filters, Client




txt = [
"نعم الله عليك ينوحي 🥺✨"
        ]
@app.on_message(filters.command(["نعم"], ""), group=73)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")
        
        
thxt = [
" بدمنك 🌝💜"
        ]
@app.on_message(filters.command(["بعشقك"], ""), group=173)

async def caesar(client: Client, message: Message):

      a = random.choice(thxt)

      await message.reply(

        f"{a}")        
     
htxt = [
"مساء العسل ينوحي🥺✨"
        ]

@app.on_message(filters.command(["مساء الخير"], ""), group=273)

async def caesar(client: Client, message: Message):

      a = random.choice(htxt)

      await message.reply(

        f"{a}")   
        
htt = [
"بنتعلم منك ينوحي 🥺✨"
        ]

@app.on_message(filters.command(["عبيط"], ""), group=373)

async def caesar(client: Client, message: Message):

      a = random.choice(htt)

      await message.reply(

        f"{a}")           
        
htx = [
"مفيش خرا غيرك 🤓✨"
        ]

@app.on_message(filters.command(["خرا"], ""), group=253)

async def caesar(client: Client, message: Message):

      a = random.choice(htx)

      await message.reply(

        f"{a}")        

ht = [
"بلموز 🤓✨"
        ]

@app.on_message(filters.command(["هلوز"], ""), group=673)

async def caesar(client: Client, message: Message):

      a = random.choice(ht)

      await message.reply(

        f"{a}")

hxt = [
" نعم الله عليك 🌚❤️"
        ]

@app.on_message(filters.command(["$/($-%-%+$♥"], ""), group=2073)

async def caesar(client: Client, message: Message):

      a = random.choice(txt)

      await message.reply(

        f"{a}")

hytxt = [
"شبهك كدا 🥺✨"
        ]

@app.on_message(filters.command(["قمر"], ""), group=2173)

async def caesar(client: Client, message: Message):

      a = random.choice(hytxt)

      await message.reply(

        f"{a}")   

hgtxt = [
"قلبظ بجنيه 🤓✨"
        ]

@app.on_message(filters.command(["ايه"], ""), group=2573)

async def caesar(client: Client, message: Message):

      a = random.choice(hgtxt)

      await message.reply(

        f"{a}")   

ghtxt = [
"اكتر ينوحي 🥺✨"
        ]

@app.on_message(filters.command(["حبق"], ""), group=26773)

async def caesar(client: Client, message: Message):

      a = random.choice(ghtxt)

      await message.reply(

        f"{a}")   

jhtxt = [
"اسمها السلام عليكم يا جحش"
        ]

@app.on_message(filters.command(["سمو عليكو"], ""), group=26473)

async def caesar(client: Client, message: Message):

      a = random.choice(jhtxt)

      await message.reply(

        f"{a}")   

ahtxt = [
"اكتب البوب هيجيلك يعمو 🥺✨"
        ]

@app.on_message(filters.command(["مين المطور"], ""), group=23573)

async def caesar(client: Client, message: Message):

      a = random.choice(ahtxt)

      await message.reply(

        f"{a}")   

shtxt = [
"كداب 🤓✨"
        ]

@app.on_message(filters.command(["اتفق"], ""), group=29773)

async def caesar(client: Client, message: Message):

      a = random.choice(shtxt)

      await message.reply(

        f"{a}")   

dhtxt = [
"لو سرقت اعضاء هزنقك 🙂✨"
        ]

@app.on_message(filters.command(["بايو"], ""), group=2873)

async def caesar(client: Client, message: Message):

      a = random.choice(dhtxt)

      await message.reply(

        f"{a}")
mhtxt = [
"يقدع قول كلام غير دا🤓✨"
        ]

@app.on_message(filters.command(["ضحك"], ""), group=2601473)

async def caesar(client: Client, message: Message):

      a = random.choice(mhtxt)

      await message.reply(

        f"{a}")   

lhtxt = [
"شخ بعيد يمعفن😹😹💔"
        ]

@app.on_message(filters.command(["بشخ"], ""), group=231673)

async def caesar(client: Client, message: Message):

      a = random.choice(lhtxt)

      await message.reply(

        f"{a}")   

xhtxt = [
"يلا من هنا يا شويه شواذ🤓✨"
        ]

@app.on_message(filters.command(["شاذ"], ""), group=274683)

async def caesar(client: Client, message: Message):

      a = random.choice(xhtxt)

      await message.reply(

        f"{a}")   

dfhtxt = [
"صباح العسل ينوحي 🥺✨"
        ]

@app.on_message(filters.command(["صباح الخير"], ""), group=2756033)

async def caesar(client: Client, message: Message):

      a = random.choice(dfhtxt)

      await message.reply(

        f"{a}")   

nhtxt = [
"فلل عليك يحب🤓✨"
        ]

@app.on_message(filters.command(["فل"], ""), group=207973)

async def caesar(client: Client, message: Message):

      a = random.choice(nhtxt)

      await message.reply(

        f"{a}")   

phtxt = [
"عايز منها اي😒🤌"
        ]

@app.on_message(filters.command(["انجل"], ""), group=270973)

async def caesar(client: Client, message: Message):

      a = random.choice(phtxt)

      await message.reply(

        f"{a}")   

ihtxt = [
"النسونجي الكبير🤓✨"
        ]

@app.on_message(filters.command(["نينجا"], ""), group=273573)

async def caesar(client: Client, message: Message):

      a = random.choice(ihtxt)

      await message.reply(

        f"{a}")   

uhtxt = [
"النسونجي الصغير🤓✨"
        ]

@app.on_message(filters.command(["حمو بني سويف"], ""), group=279373)

async def caesar(client: Client, message: Message):

      a = random.choice(uhtxt)

      await message.reply(

        f"{a}")   

rhtxt = [
"يارب يكون الموضوع مهم"
        ]

@app.on_message(filters.command(["بقولك"], ""), group=267473)

async def caesar(client: Client, message: Message):

      a = random.choice(rhtxt)

      await message.reply(

        f"{a}")   

htxtk = [
"قلبي🥺✨"
        ]

@app.on_message(filters.command(["روحي"], ""), group=225973)

async def caesar(client: Client, message: Message):

      a = random.choice(htxtk)

      await message.reply(

        f"{a}")

asdhtxt = [
"عيب يبني انا بوت محطرم🤓✨"
        ]

@app.on_message(filters.command(["متيجي"], ""), group=200873)

async def caesar(client: Client, message: Message):

      a = random.choice(asdhtxt)

      await message.reply(

        f"{a}")   

pokghtxt = [
" ده نورك ي قلبي🥺✨"
        ]

@app.on_message(filters.command(["نورت"], ""), group=200173)

async def caesar(client: Client, message: Message):

      a = random.choice(pokghtxt)

      await message.reply(

        f"{a}")   

ijkhtxt = [
" اي الثقافه دي 😒😹"
        ]

@app.on_message(filters.command(["وات"], ""), group=200273)

async def caesar(client: Client, message: Message):

      a = random.choice(ijkhtxt)

      await message.reply(

        f"{a}")   

kghtxt = [
"بلحه🤓✨"
        ]

@app.on_message(filters.command(["السيسي"], ""), group=200373)

async def caesar(client: Client, message: Message):

      a = random.choice(kghtxt)

      await message.reply(

        f"{a}")   

lphtxt = [
"طرح"
        ]

@app.on_message(filters.command(["بلح"], ""), group=200473)

async def caesar(client: Client, message: Message):

      a = random.choice(lphtxt)

      await message.reply(

        f"{a}")   

tthtxt = [
" يا ثقافتك يجدع😹😹💔"
        ]

@app.on_message(filters.command(["ثانكس"], ""), group=200573)

async def caesar(client: Client, message: Message):

      a = random.choice(tthtxt)

      await message.reply(

        f"{a}")   

qqhtxt = [
" انت الي كيكه ياقمر 🤤🌝"
        ]

@app.on_message(filters.command(["كيكه"], ""), group=200673)

async def caesar(client: Client, message: Message):

      a = random.choice(qqhtxt)

      await message.reply(

        f"{a}")   

wwhtxt = [
"مين اتجوز🤓✨"
        ]

@app.on_message(filters.command(["هيصا"], ""), group=200773)

async def caesar(client: Client, message: Message):

      a = random.choice(wwhtxt)

      await message.reply(

        f"{a}")   

zzhtxt = [
"فرح خالتك قريب 😹💋💃🏻"
        ]

@app.on_message(filters.command(["طيب"], ""), group=200873)

async def caesar(client: Client, message: Message):

      a = random.choice(zzhtxt)

      await message.reply(

        f"{a}")   

vvhtxt = [
"مالك يتي🥺✨"
        ]

@app.on_message(filters.command(["اخخ"], ""), group=200973)

async def caesar(client: Client, message: Message):

      a = random.choice(vvhtxt)

      await message.reply(

        f"{a}")   

xxhtxt = [
"ثكر 🥺✨"
        ]

@app.on_message(filters.command(["يتي"], ""), group=2000173)

async def caesar(client: Client, message: Message):

      a = random.choice(xxhtxt)

      await message.reply(

        f"{a}")   

cchtxt = [
"انا لو اعرف مش هقولك🙂✨"
        ]

@app.on_message(filters.command(["مين هنا"], ""), group=2000273)

async def caesar(client: Client, message: Message):

      a = random.choice(cchtxt)

      await message.reply(

        f"{a}")   

kjjhtxt = [
"مالك يتي🤓✨"
        ]

@app.on_message(filters.command(["هييح"], ""), group=2000373)

async def caesar(client: Client, message: Message):

      a = random.choice(kjjhtxt)

      await message.reply(

        f"{a}")   

ffhtxt = [
"عيب نا بوت محطرم🥺✨"
        ]

@app.on_message(filters.command(["اقلع"], ""), group=2000473)

async def caesar(client: Client, message: Message):

      a = random.choice(ffhtxt)

      await message.reply(

        f"{a}")   

pphtxt = [
"اسم امك دا ولا اي 😹😹"
        ]

@app.on_message(filters.command(["احلام"], ""), group=2000573)

async def caesar(client: Client, message: Message):

      a = random.choice(pphtxt)

      await message.reply(

        f"{a}")   

oohtxt = [
" يتي يتي 🌝😂"
        ]

@app.on_message(filters.command(["عبيبي"], ""), group=20703)

async def caesar(client: Client, message: Message):

      a = random.choice(oohtxt)

      await message.reply(

        f"{a}")   

llhtxt = [
"لا متسيبوش ليزنقك🤓✨"
        ]

@app.on_message(filters.command(["سيبو"], ""), group=20713)

async def caesar(client: Client, message: Message):

      a = random.choice(llhtxt)
      
      await message.reply(

        f"{a}")   

kkhtxt = [
"الهوا شلحا🤓✨"
        ]

@app.on_message(filters.command(["احا"], ""), group=22703)

async def caesar(client: Client, message: Message):

      a = random.choice(kkhtxt)

      await message.reply(

        f"{a}")   
 